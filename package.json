{
  "name": "bneidavid-lessons-downloader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "yarn fe:build",
    "fe:dev": "cd frontend && yarn start",
    "fe:build": "cd frontend && yarn build",
    "fe:installDeps": "cd frontend && yarn install",
    "be:serve": "python backend/app.py",
    "be:dev": "yarn be:serve --dev",
    "production": "yarn fe:build && yarn be:serve",
    "deploy": "yarn fe:installDeps && yarn production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dev-dependencies": {
    "concurrently": "^5.0.0"
  }
}
